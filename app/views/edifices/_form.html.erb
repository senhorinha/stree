<%= form_for(@edifice) do |f| %>
  <% if @edifice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edifice.errors.count, "error") %> prohibited this edifice from being saved:</h2>

      <ul>
      <% @edifice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sustainability_level %><br>
    <%= f.number_field :sustainability_level %>
  </div>
  <div class="field">
    <%= f.label :last_update_of_sustainability_level %><br>
    <%= f.date_select :last_update_of_sustainability_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
